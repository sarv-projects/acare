safety:
  current_limit_A:            8.0
  current_warning_A:          6.0
  temperature_estop_C:       75.0
  temperature_slow_C:        65.0
  temperature_warning_C:     55.0
  velocity_limit_degs:      120.0
  velocity_operational_degs: 80.0
  lidar_caution_mm:          600
  lidar_stop_mm:             400
  gripper_force_limit_N:     15.0
  gripper_force_warning_N:   10.0

fake_detection:
  # Starting estimates — MUST calibrate empirically (admin.py calibrate Step 6)
  # Place 20 real tools + 20 printed replicas, record values, find threshold
  texture_variance_threshold: 120.0
  depth_variance_threshold:   0.002
